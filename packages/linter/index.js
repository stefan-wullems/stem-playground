const core = require('./lib/core').default
const react = require('./lib/react').default
const testing = require('./lib/testing').default

module.exports = {
  configs: {
    core,
    react,
    testing
  },
}
